<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="#E0F7FA"
    android:padding="20dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Input: длина пароля -->
        <com.google.android.material.textfield.TextInputLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:startIconDrawable="@drawable/ic_password"
            android:hint="Длина пароля (от 4 до 32 )"
            android:layout_marginBottom="12dp">

            <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/passwordLengthInput"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="number"
                android:maxLength="2"
                android:textColor="#000000"
                android:contentDescription="Длина пароля"/>
        </com.google.android.material.textfield.TextInputLayout>

        <!-- Чекбоксы: иконка слева, галочка справа -->
        <com.google.android.material.checkbox.MaterialCheckBox
            android:id="@+id/useSpecialSymbols"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Использовать специальные символы"
            android:drawableStart="@drawable/ic_symbols"
            android:drawablePadding="12dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:contentDescription="Использовать специальные символы" />

        <com.google.android.material.checkbox.MaterialCheckBox
            android:id="@+id/useLowercase"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Использовать строчные буквы"
            android:drawableStart="@drawable/ic_lowercase"
            android:drawablePadding="12dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:contentDescription="Использовать строчные буквы" />

        <com.google.android.material.checkbox.MaterialCheckBox
            android:id="@+id/useUppercase"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Использовать заглавные буквы"
            android:drawableStart="@drawable/ic_uppercase"
            android:drawablePadding="12dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:contentDescription="Использовать заглавные буквы" />

        <!-- Результат -->
        <TextView
            android:id="@+id/passwordOutput"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Пароль появится здесь"
            android:textSize="18sp"
            android:textColor="#3D3D3D"
            android:layout_marginTop="24dp"
            android:layout_marginBottom="12dp"
            android:contentDescription="Вывод пароля" />

        <!-- Кнопки -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/generateButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="СГЕНЕРИРОВАТЬ"
            android:layout_marginTop="8dp"
            app:icon="@drawable/ic_generate"
            app:iconGravity="textStart"
            app:iconPadding="8dp"
            app:iconTint="@android:color/white"
            style="@style/Widget.MaterialComponents.Button" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/copyButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="КОПИРОВАТЬ"
            android:layout_marginTop="8dp"
            app:icon="@drawable/ic_copy"
            app:iconGravity="textStart"
            app:iconPadding="8dp"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton" />

    </LinearLayout>
</androidx.core.widget.NestedScrollView>
